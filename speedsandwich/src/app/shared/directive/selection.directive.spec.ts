import { SelectionDirective } from './selection.directive';

describe('SelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionDirective();
    expect(directive).toBeTruthy();
  });
});
